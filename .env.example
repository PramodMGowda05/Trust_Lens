# This file contains example environment variables.
# Copy this file to .env and fill in the values for your local environment.

# --- Database Configuration ---
# The host for your MySQL database. For local development, this is usually 127.0.0.1.
DB_HOST=127.0.0.1
# The username for your MySQL database. For local development, this is often 'root'.
DB_USER=root
# The password for your MySQL user.
DB_PASSWORD=
# The name of the database to use.
DB_NAME=trustlens_db

# --- Authentication ---
# A long, random, and secret string used to sign JSON Web Tokens (JWTs).
# You can generate one using a UUID generator or a random string generator.
JWT_SECRET=
